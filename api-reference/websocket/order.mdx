---
title: "Order"
---

Receive real-time updates on your orders.

- Topic: `order`
- Stream: Private (`wss://api.ekiden.fi/ws/private`)

## Authenticate

Use authentication flow described in the [Connect](./connect) page.

## Subscribe

```json
{ "op": "subscribe", "args": ["order"], "req_id": "201001" }
```

Ack:

```json
{ "op": "subscribed", "args": ["order"], "req_id": "201001" }
```

## Events

Messages arrive with `op: "event"`, `topic: "order"`, and `data` is an array of `Order`.

Example:

```json
{
	"op": "event",
	"topic": "order",
	"server_ts_ms": 1731541800000,
	"data": [
		{
			"sid": "0xord1",
			"side": "buy",
			"size": "1000",
			"price": "100200",
			"leverage": 5,
			"initial_margin": "250",
			"type": "limit",
			"status": "placed",
			"user_addr": "0xuser...",
			"market_addr": "0x88a70ff...",
			"is_cross": true,
			"seq": 456,
			"timestamp": 1681234567,
			"timestamp_ms": 1681234567000
		}
	]
}
```

Field notes:

- `timestamp` (seconds) is deprecated; prefer `timestamp_ms`.
- `size`, `price`, `initial_margin` are strings to preserve precision.

### Fields

| Field | Type | Notes |
|-------|------|-------|
| sid | string | Order identifier |
| side | string | `buy` or `sell` |
| size | string | Base units (scaled) |
| price | string | Scaled price |
| leverage | integer | Requested leverage |
| initial_margin | string or null | Present when computed, else null |
| type | string | Order type (limit, market, etc.) |
| status | string | placed, filled, cancelled, etc. |
| user_addr | string | User account address |
| market_addr | string | Market address |
| is_cross | boolean | Cross vs isolated margin |
| seq | integer | Sequence of creation batch |
| timestamp | integer | Deprecated seconds timestamp |
| timestamp_ms | integer | Milliseconds timestamp (preferred) |

See schema details in [AsyncAPI](../websockets/private): `Order`.
