---
title: 'Ekiden SDK'
description: 'Get started with our SDKs to interact with the Ekiden Gateway.'
---

## SDK Index

Select the language:

- [TypeScript SDK](https://github.com/ekidenfi/ekiden-ts-sdk)
- Rust SDK
- Python SDK

## Getting Started

Each SDK provides:

- Secure access to the Ekiden Gateway
- Vault interaction utilities
- Order and market handling

Choose your preferred SDK above to get started.


# TypeScript SDK

TypeScript SDK for interacting with the Ekiden Gateway and Aptos DeFi protocol.

## Installation

```bash
# With your preferred package manager:
pnpm add @ekiden/ts-sdk
npm install @ekiden/ts-sdk
yarn add @ekiden/ts-sdk
bun add @ekiden/ts-sdk
```

## Quick Example

```typescript
import { EkidenClient, TESTNET } from '@ekiden/ts-sdk'

const ekiden = new EkidenClient(TESTNET)
const { token } = await ekiden.authorize({
  signature: '0x...',
  public_key: '0x...',
})
ekiden.setToken(token)
const markets = await ekiden.getMarkets()
```

---

## Learn More

For full usage details, API reference, and examples, check the GitHub repo:

➡️ [ekiden-ts-sdk on GitHub](https://github.com/ekidenfi/ekiden-ts-sdk)

For any questions or issues, feel free to open a GitHub issue.